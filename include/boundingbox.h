// Defines the bounding box of a triangle
// Useful to check if a point is within a bounding box, other functionalities added later

#pragma once
class BoundingBox {
   public:
    // calculate center point
    // calculate all 4 points
    // calculate if a point intersects this bounding box
   private:
    // only two points needed for axis-alligned bounding box
};
